# Note: This meson recipe is taken from poky master branch (git://git.yoctoproject.org/poky), since dunfell or morty has older version than required.
# This recipe has to be removed once the poky branches updated with required versions.

include meson.inc

BBCLASSEXTEND = "native"

